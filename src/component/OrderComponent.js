import styled from "styled-components"

const OrderDiv = styled.div`

`;
export default function OrderComponent() {

    return <OrderDiv>
        <button>ASC</button>
        <button>DESC</button>
    </OrderDiv>
}